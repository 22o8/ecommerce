import { prisma } from '../../utils/db'
import { requirePermission } from '../../utils/auth'
function fmt(v:any,c='IQD'){ const n=Number(v||0).toLocaleString('en-US'); return c==='USD' ? `$ ${n}` : `${n} د.ع` }
export default defineEventHandler(async(event)=>{ await requirePermission(event,'invoices'); const [setting,invoices]=await Promise.all([prisma.dealerSetting.findUnique({where:{id:1}}), prisma.invoice.findMany({orderBy:{invoiceDate:'desc'}, take:200})]); const rows=invoices.map(i=>`<tr><td>${i.invoiceNumber}</td><td>${i.invoiceType}</td><td>${i.customerName}</td><td>${i.title}</td><td>${fmt(i.amount,i.currency)}</td><td>${new Date(i.invoiceDate).toLocaleDateString('ar-IQ')}</td></tr>`).join(''); const doc=`<!doctype html><html dir="rtl" lang="ar"><head><meta charset="utf-8"><title>كل الفواتير</title><style>body{font-family:Tahoma,Arial;margin:30px;color:#111}h1{margin:0}.muted{color:#666}table{width:100%;border-collapse:collapse;margin-top:25px}th,td{border:1px solid #ddd;padding:10px;text-align:right}th{background:#0f2a4a;color:white}.actions{text-align:left;margin-bottom:20px}.btn{padding:12px 18px;background:#2563eb;color:white;border:0;border-radius:8px;font-weight:bold}@media print{.actions{display:none}}</style></head><body><div class="actions"><button class="btn" onclick="window.print()">طباعة أو حفظ PDF</button></div><h1>${setting?.dealerName||'AutoDealer Pro'}</h1><div class="muted">تقرير كل الفواتير والسندات</div><table><thead><tr><th>رقم الفاتورة</th><th>النوع</th><th>العميل</th><th>العنوان</th><th>المبلغ</th><th>التاريخ</th></tr></thead><tbody>${rows}</tbody></table></body></html>`; setHeader(event,'content-type','text/html; charset=utf-8'); return doc })
