import { prisma } from '../../../utils/db'
import { requirePermission } from '../../../utils/auth'
function fmt(v:any,c='IQD'){ const n=Number(v||0).toLocaleString('en-US'); return c==='USD' ? `$ ${n}` : `${n} د.ع` }
function html(inv:any, setting:any){ const sale=inv.sale; return `<!doctype html><html dir="rtl" lang="ar"><head><meta charset="utf-8"><title>${inv.invoiceNumber}</title><style>body{font-family:Tahoma,Arial,sans-serif;margin:0;background:#f4f6f8;color:#111}.page{max-width:900px;margin:30px auto;background:white;padding:36px;border:1px solid #ddd}.head{display:flex;justify-content:space-between;gap:20px;border-bottom:3px solid #0f2a4a;padding-bottom:18px}.brand h1{margin:0;font-size:28px}.muted{color:#667085}.title{text-align:center;margin:30px 0;font-size:26px;font-weight:900}.grid{display:grid;grid-template-columns:1fr 1fr;gap:12px}.box{border:1px solid #e5e7eb;border-radius:10px;padding:12px}.label{font-size:12px;color:#667085}.value{font-size:18px;font-weight:800;margin-top:5px}table{width:100%;border-collapse:collapse;margin-top:22px}th,td{border:1px solid #e5e7eb;padding:12px;text-align:right}th{background:#0f2a4a;color:white}.total{font-size:22px;font-weight:900;text-align:left;margin-top:18px}.sign{display:flex;justify-content:space-between;margin-top:60px}.actions{max-width:900px;margin:20px auto;text-align:left}.btn{padding:12px 18px;background:#2563eb;color:white;border-radius:8px;border:0;font-weight:bold;cursor:pointer}@media print{.actions{display:none}.page{margin:0;max-width:none;border:0}}</style></head><body><div class="actions"><button class="btn" onclick="window.print()">طباعة أو حفظ PDF</button></div><div class="page"><div class="head"><div class="brand"><h1>${setting?.dealerName||'AutoDealer Pro'}</h1><div class="muted">${setting?.address||''}</div><div class="muted">${setting?.phone||''}</div></div><div><div class="label">رقم المستند</div><div class="value">${inv.invoiceNumber}</div><div class="label">التاريخ</div><div class="value">${new Date(inv.invoiceDate).toLocaleDateString('ar-IQ')}</div></div></div><div class="title">${inv.invoiceType}</div><div class="grid"><div class="box"><div class="label">اسم العميل أو الجهة</div><div class="value">${inv.customerName||'-'}</div></div><div class="box"><div class="label">رقم الهاتف</div><div class="value">${inv.customerPhone||'-'}</div></div><div class="box"><div class="label">العنوان أو السبب</div><div class="value">${inv.title}</div></div><div class="box"><div class="label">طريقة الدفع</div><div class="value">${inv.paymentMethod||'نقدي'}</div></div></div><table><thead><tr><th>البيان</th><th>المبلغ</th><th>العملة</th></tr></thead><tbody><tr><td>${inv.title}</td><td>${Number(inv.amount||0).toLocaleString('en-US')}</td><td>${inv.currency==='USD'?'دولار':'دينار عراقي'}</td></tr>${sale?`<tr><td>المدفوع</td><td>${Number(sale.paidAmount||0).toLocaleString('en-US')}</td><td>${sale.currency==='USD'?'دولار':'دينار عراقي'}</td></tr><tr><td>المتبقي</td><td>${Number(sale.remainingAmount||0).toLocaleString('en-US')}</td><td>${sale.currency==='USD'?'دولار':'دينار عراقي'}</td></tr>`:''}</tbody></table><div class="total">الإجمالي: ${fmt(inv.amount,inv.currency)}</div><p class="muted">${inv.notes||''}</p><div class="sign"><div>توقيع المستلم</div><div>ختم وتوقيع المعرض</div></div></div></body></html>` }
export default defineEventHandler(async(event)=>{ await requirePermission(event,'invoices'); const id=getRouterParam(event,'id')!; const inv=await prisma.invoice.findUnique({where:{id}, include:{sale:{include:{car:true,customer:true,installments:true,payments:true}}}}); if(!inv) throw createError({statusCode:404,message:'الفاتورة غير موجودة'}); const setting=await prisma.dealerSetting.findUnique({where:{id:1}}); const type=(getQuery(event).type as string)||'html'; const content=html(inv,setting); setHeader(event,'content-type', type==='word'?'application/msword; charset=utf-8':'text/html; charset=utf-8'); if(type==='word') setHeader(event,'content-disposition',`attachment; filename=${inv.invoiceNumber}.doc`); return content })
